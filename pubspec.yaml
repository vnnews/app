name: vnnews
description: Vietnamese News companion app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  cloud_firestore: "^0.16.0"
  firebase_core: "0.7.0"
  flutter:
    sdk: flutter
  flutter_hooks:
  flutter_widget_from_html:
  freezed_annotation:
  hooks_riverpod:

dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter
  freezed:
  json_serializable:

flutter:
  uses-material-design: true
